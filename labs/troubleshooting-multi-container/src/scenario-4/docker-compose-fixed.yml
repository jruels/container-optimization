# Scenario 4: FIXED - Correct volume mount path and working nginx config

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      # FIX: Mount to correct path and use working config
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api

  api:
    build: ../python-app
    environment:
      - REDIS_HOST=redis

  redis:
    image: redis:7-alpine
