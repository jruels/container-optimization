# Scenario 2: FIXED - Both containers on the same network

services:
  api:
    build: ../go-app
    ports:
      - "8080:8080"
    environment:
      - REDIS_HOST=redis
    networks:
      - app-network   # FIX: Use same network as Redis

  redis:
    image: redis:7-alpine
    networks:
      - app-network   # FIX: Use same network as API

networks:
  app-network:   # FIX: Single shared network
